# # This is a basic workflow to help you get started with Actions

# name: CI build images

# # Controls when the workflow will run
# on: [push]

# env:
#   DOCKER_USER: ${{secrets.DOCKERHUB_REPO_USER}}
#   DOCKER_PASSWORD: ${{secrets.DOCKERHUB_REPO_PW}}
#   REPO_NAME: ${{secrets.DOCKER_REPO_NAME}}

# # A workflow run is made up of one or more jobs that can run sequentially or in parallel
# jobs:
#   # This workflow contains a single job called "build"
#   build:
#     # The type of runner that the job will run on
#     runs-on: ubuntu-latest

#     # Steps represent a sequence of tasks that will be executed as part of the job
#     steps:
#     - uses: actions/checkout@v3 # first action : checkout source code

#     - name: docker login
#       run: | # log into docker hub account
#         docker login -u $DOCKER_USER -p $DOCKER_PASSWORD

#     - name: Get current date # get the date of the build
#       id: date
#       run: echo "::set-output name=date::$(date +'%Y-%m-%d--%M-%S')"

#     - name: Build the Docker image # push The image to the docker hub
#       run: docker build . --file Dockerfile --tag $DOCKER_USER/$REPO_NAME:cilatest #${{ steps.date.outputs.date }}

#     - name: Docker Push
#       run: docker push $DOCKER_USER/$REPO_NAME:cilatest   #${{ steps.date.outputs.date }}
